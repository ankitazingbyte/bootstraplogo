<%= form_for @about do |form| %>
  <% if @about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 
    <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title, id: :about_title %>
    </div>
    <div class="column">
      <div class="field">
        <div class="col-lg-10">
          <%= form.label :text %>
          <%= form.text_area :text, :class => 'form-control ckeditor' %>
        </div>
      </div>
      <div class="field">
        <div class="col-lg-10">
          <%= form.label :avatar %>
          <%= form.file_field :avatar %>
        </div>
      </div>

      <div class="field">
        <div class="col-lg-10">
          <%= form.label :description %>
          <%= form.text_area :description, :class => 'form-control ckeditor' %>
        </div>
      </div>
      <div class="col-lg-10">
        <div class="actions">
          <%= form.submit %>
        </div>
      </div>
    </div>
<% end %>
